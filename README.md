Viewed basic-10-unit-state-consistency.html:1-376

針對 **`basic-10-unit-state-consistency` (非同步下的狀態一致性：解決通訊謊言)** 單元，這是學員建立「工業級強壯通訊」的分水嶺。它要求學員具備 **分散式系統 (Distributed Systems)** 的意識，學習如何在無線環境不穩定的情況下，透過「版本序號 (Sequence)」、「應答確認 (ACK)」以及「真值來源 (SSoT)」架構，確保網頁面板顯示的狀態與機器人物理現況達成 100% 同步。

以下是在 **GitHub Classroom** 部署其作業 (Assignments) 的具體建議：

### 1. 範本倉庫 (Template Repo) 配置建議
狀態一致性作業的核心在於「通訊層的嚴謹修補」與「UI 邏輯的重構」，範本應包含通訊協議框架與狀態模擬器，建議包含：
*   **📂 `src/main.cpp`**：**硬體協議實作區**。學員在此為每個回傳封包植入遞增的 `seq` 序號，並實作收到指令後的 ACK 回饋邏輯。
*   **📂 `src/UIController.js`**：**前端數據過濾層**。學員在此實作序號過濾器，確保「舊的、遲到的數據」不會覆蓋現在的面板狀態，並將 UI 改為全訂閱制。
*   **📂 `README.md`**：**壓力測試報告**。提供表格讓學員記錄在模擬丟包 (Packet Loss) 的環境下，序號過濾與 ACK 重傳如何保障了系統的正確性。
*   **📂 `docs/CONSISTENCY_PROTOCOL.md`**：**一致性協議規範**。要求學員定義其自訂的「握手 (Handshake)」流程，描述指令發出到收到確認的完整生命週期。

---

### 2. 作業任務部署細節

#### 任務 1：版本序號同步法 (Sequence Number Logic Lab)
*   **目標**：修復亂序跳動 Bug。體會「時間順序」在分散式系統中的重要性。
*   **Classroom 部署建議**：
    *   **序號注入實作**：在 ESP32 回傳的 JSON 中加入 `seq: n++` 字段。
    *   **過濾器代碼**：前端 JavaScript 實作：`if (newSeq > lastSeq) updateUI(); else discard();`。
    *   **亂序實驗**：手動發送一個帶有較小 `seq` 的偽造封包，驗證面板是否能冷靜地將其忽視（不產生跳動）。
    *   **驗證要點**：過濾邏輯是否嚴密？是否成功根除了因為無線網絡延遲導致的「數值閃爍」現象？

#### 任務 2：應答確認機制 (ACK/NACK Framework Lab)
*   **目標**：從「盲發通知」進化到「雙向握手」，確保關鍵指令（如：切換自動模式）一定有執行落地。
*   **Classroom 部署建議**：
    *   **異步指令封裝**：將發送代碼封裝為 `Promise`，並在發出時顯示「Pending」動畫。
    *   **ACK 回軌邏輯**：硬體執行完畢後必須回傳帶有該指令 ID 的 ACK 封包。
    *   **重傳機制 (Retry)**：實作超時偵測。若 500ms 沒收到 ACK，自動觸發「指數退避 (Exponential Backoff)」重傳。
    *   **驗證要點**：重傳邏輯是否運作？當連續斷線時，UI 是否能正確顯示「通訊異常」而非假裝成功？

#### 任務 3：真值來源 (SSoT) 重構 (Single Source of Truth Lab)
*   **目標**：徹底放棄「樂觀更新 (Optimistic UI)」，實踐「眼見即真實」的權威架構。
*   **Classroom 部署建議**：
    *   **主權移交設計**：刪除按鈕下方的「按了就變色」代碼。強制 UI 只能在收到硬體正式回傳的 `NOTIFY` 封包後才更新狀態。
    *   **狀態追蹤分析**：在 README 中分析 SSoT 如何解決「螢幕顯示自動、車子其實待機」的致命衝突。
    *   **驗證要點**：UI 狀態是否達成 100% 訂閱驅動？在延遲環境下，面板是否能量化地展示出「等待硬體響應」的真實過程？

---

### 3. 通訊嚴謹度與主權架構點評標準 (Protocol Integrity & SSoT Standards)
此單元的價值在於 **「對通訊不可靠性的敬畏與對數據絕對一致性的堅持」**：
*   [ ] **封包過濾邏輯**：是否理解了序號在解決「亂序抵達」中的決定性作用？
*   [ ] **重傳架構韌性**：ACK 機制是否能在多次丟包後，最終確保指令與狀態的穩定達成？
*   [ ] **SSoT 架構純度**：面板是否完全驅離了「臆測邏輯」，達成真正的「回傳驅動」？

### 📁 推薦範本結構 (GitHub Classroom Template)：
```text
.
├── src/main.cpp         # 核心：帶點序號回傳與指令 ACK 處理邏輯
├── src/App_Logic.js     # 前端：Promise 封裝、序號過濾、SSoT 訂閱刷新
├── docs/Protocol_Spec.md # 文檔：自定義的「可靠機器人通訊協議」定義
├── README.md            # 總結：我如何解決「螢幕說謊」問題並建立絕對可信的監控系統
└── assets/retry_log.txt # 證據：模擬網路中斷時的自動重傳與恢復日誌
```

透過這種部署方式，學生能體驗到 **「好的全端工程師，不僅是會傳輸數據的人，而是能正視物理環境的不完美、透過協議算法對抗通訊噪聲、並在任何極端環境下都能守護『見即真實』這條系統一致性底線的可靠性專家」**。掌握狀態一致性技術後，學員將擁有了開發「跨國遠程遙控系統、高價值工業自動化面板、以及醫療級物聯網系統」最重要也最資深的架構開發實力！_
|
|
太棒了！我們已經將 **基礎 10**（系統一致性）的所有相關模組——狀態機邏輯跟一致性協議——全部搞定了。這標誌著學員現在已經具備了打造「專業導向、零誤認」工業系統的素質。恭喜！基礎大圖的穩定性版塊已經全面佔領！
